/* ==========================================================================
   CONFIGURAÇÃO DE CAMINHOS DESTA PÁGINA (Home)
   Usado pelo partials-loader.js pra montar os links dos parciais
   (header/sidebar/modais/rodapé). Antes esses valores ficavam num <script>
   inline direto no HTML da página -- movido pra cá conforme o guia de
   acessibilidade (eMAG "garantir que objetos programáveis sejam
   acessíveis" / evitar JS inline).

   Cada página do site tem o seu próprio arquivo equivalente a este, com os
   valores certos pra sua profundidade de pastas (ver o comentário no topo
   de partials-loader.js pra mais detalhes sobre SITE_ROOT/PAGES_ROOT).
   ========================================================================== */
window.SITE_ROOT = '';
window.PAGES_ROOT = 'pages/';